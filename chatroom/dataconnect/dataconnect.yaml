specVersion: "v1beta"
serviceId: "chatroom"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "chatroom-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
